# Build tooling boundary

No production build script is active.

Future scripts must be deterministic, repository-relative, and unable to deploy. Build output alone cannot authorize release. Pipeline implementation starts only after canonical contract review and a separate authorization.
