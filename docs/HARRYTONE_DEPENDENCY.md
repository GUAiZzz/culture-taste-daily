# HarryTone dependency

Canonical HarryTone remains in the private repository `GUAiZzz/harry-tone`.

This public repository records dependency identity only:

- repository: `GUAiZzz/harry-tone`
- branch: `main`
- pinned commit: `13cd6b7046bd81be396810d2923f3a5dc818e93f`

The machine-readable reference is at `dependencies/harrytone.json`.

No HarryTone instruction file, skill source, private reference, or repository snapshot may be copied here. A future build environment must obtain authorized access at runtime and fail closed when the pinned dependency cannot be verified. It must not invent or infer missing HarryTone rules.

The accepted `2026-08-25` candidate was produced in a `snapshot-assisted dry run`; that historical fact is disclosed in its public manifest. It does not modify canonical HarryTone and does not make the supplied snapshot canonical.
