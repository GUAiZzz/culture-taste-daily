# Culture & Taste Daily — 2026-08-25 local candidate

This folder is an isolated review candidate. It has not been pushed, deployed, merged, or copied into production.

## Open locally

Serve this folder with any static HTTP server, then open:

- `/` for the local candidate cover
- `/issues/2026-08-25/` for the issue
- `/archive/` for the archive candidate

The issue remains fully readable without JavaScript. JavaScript only updates the sticky room index.

## Evidence

- `issues/2026-08-25/content.md` is the locked content source.
- `issues/2026-08-25/issue-manifest.public.json` is publishable metadata and reporting only.
- `qa/deterministic-report.json` records reproducible technical checks.
- `previews/` contains desktop, mobile, no-JavaScript, and reduced-motion renders.

Private source notes, rejected candidates, reference-case evidence, and editorial/visual review are intentionally excluded from this public candidate folder.

## Boundary

Manifest and QA `PASS` values cannot authorize deployment. This candidate uses the proposed V3 contract for an isolated dry run only.
